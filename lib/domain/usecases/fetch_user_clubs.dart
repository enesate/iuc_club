import 'package:iuc_club/data/repositories/user_repositor.dart';

class FetchUserClubs {
  final UserRepository repository;

  FetchUserClubs(this.repository);

  /* Future<List<Map<String, dynamic>>> execute(String userId) async {
    return await repository.fetchUserClubs(userId);
  } */
}
